# Repo conventions

## Commit / PR attribution
Never add Claude or Anthropic attribution to anything pushed to this repo: no
`Co-Authored-By: Claude ...` trailers, no `Claude-Session:` links, no model names
(e.g. "Sonnet", "Opus"), in commit messages, PR titles/bodies, or code comments.
Author commits as the user only.
